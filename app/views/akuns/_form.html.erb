<%= form_for(@akun, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @akun.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@akun.errors.count, "error") %> prohibited this akun from being saved:</h4>

      <ul>
      <% @akun.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :kode, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :kode, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :nama, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :nama, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :tipe_akun, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :tipe_akun, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :user, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :user, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
